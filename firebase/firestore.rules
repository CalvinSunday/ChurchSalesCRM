rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // OPEN RULES (requested): anyone can read/write.
      // IMPORTANT: this means any person with the URL can modify your CRM data.
      allow read, write: if true;
    }
  }
}
